"""eBay Finding API — Sold Listings und Marktpreise abrufen.

Phase 3 Implementierung.
"""

import logging

logger = logging.getLogger(__name__)


def fetch_sold_listings(character: str, card_type: str, config: dict) -> list[dict]:
    """Verkaufte eBay Listings der letzten 90 Tage abrufen.

    Wird in Phase 3 implementiert.
    """
    logger.info("eBay Sold — noch nicht implementiert (Phase 3)")
    return []
